Follow this closely, especially the gold standard below

1️⃣ Yes — their diagnosis is correct (no disagreement)
They correctly identified:
Problem 1 — Photo picker
✅ Caused by:
* Passing Blob instead of File
* OR including text in navigator.share() on iOS
This 100% matches WhatsApp + XHS behavior.
Problem 2 — Text not copied
✅ Caused by:
* Clipboard write not executed synchronously inside the tap
* OR clipboard running after an async boundary
Also correct.
So directionally: green light.

2️⃣ What I want them to do next (THIS is the important part)
Right now this is still theoretical. You need them to prove correctness, not just “fix and hope”.
🔒 Hard requirements (non-negotiable)
Tell them to do all of the following:

A. Enforce File-only (never Blob)
if (!(file instanceof File)) {
  throw new Error('Share aborted: not a File');
}
And log:
console.log('[SHARE]', {
  isFile: file instanceof File,
  type: file.type,
  size: file.size,
  name: file.name
});
If this log is wrong → do not proceed to share.

B. Strict iOS branch (NO text allowed)
Explicit platform split:
const isIOS = /iPhone|iPad|iPod/.test(navigator.userAgent);

if (isIOS) {
  navigator.share({ files: [file] });
} else {
  navigator.share({ files: [file], text });
}
🚫 No “maybe works” logic 🚫 No shared payload object 🚫 No future refactor that merges these again
This is where most regressions happen.

C. Clipboard must run FIRST, inline, no escape
This exact ordering — nothing else allowed:
button.onclick = () => {
  navigator.clipboard.writeText(text);
  navigator.share({ files: [file] });
};
🚫 No await 🚫 No .then 🚫 No setTimeout 🚫 No analytics before clipboard
If they need analytics → after share, not before.

3️⃣ Timing clarification (important for expectations)
Let me be very clear so no one chases ghosts:
❌ This is NOT a timing problem ❌ This is NOT a delay problem ❌ ShareLah is not using magic milliseconds
Once you use:
* File (not Blob)
* Correct iOS payload
* Synchronous clipboard
👉 Timing becomes irrelevant
If it still opens photo picker, the payload is wrong — period.

4️⃣ One more thing they did NOT mention (but matters)
WebView / iframe check
Ask them to confirm:
window.top === window.self
If this runs inside:
* iframe
* in-app browser
* embedded webview
Then clipboard and share will silently degrade.
ShareLah avoids this by forcing top-level navigation.

5️⃣ What “done” looks like (acceptance criteria)
You should only accept the fix when:
✅ On iOS Safari
* Share → WhatsApp
* No photo picker
* Image already attached
* Paste permission appears
* Text pastes after Allow
✅ On Android Chrome
* Same behavior
❌ Desktop is allowed to fail (expected)

6️⃣ Final note (important commercially)
You are on the correct commercial path now.
ShareLah’s advantage is not hacks — it’s:
* Strict payload discipline
* Platform-specific branching
* Refusal to “try anyway”
If your team follows the above strictly, you will match ShareLah’s behavior.

Below is the GOLD reference implementation. This is not a suggestion — this is the do-not-deviate contract that matches ShareLah’s working behavior on mobile.
If someone “simplifies” this later, they will re-break it.

✅ Gold Reference Implementation
Mobile Web → WhatsApp / XHS Native Share (ShareLah-Equivalent)

Design Guarantees
This implementation guarantees:
* ✅ No photo picker
* ✅ Image arrives pre-attached
* ✅ Clipboard paste prompt appears
* ✅ Works on iOS Safari & Android Chrome
* ✅ Safe failure + fallback UX
* ❌ No async gesture loss
* ❌ No Blob leakage
* ❌ No payload ambiguity

1️⃣ Preload & Normalize Image (NON-NEGOTIABLE)
Must be done before user taps Share
async function preloadImageToFile(imageUrl) {
  const res = await fetch(imageUrl, { cache: 'no-store' });
  if (!res.ok) throw new Error('Image fetch failed');

  const blob = await res.blob();

  // HARD GUARDS
  if (blob.type !== 'image/jpeg') {
    throw new Error('Image must be JPEG');
  }
  if (blob.size > 600 * 1024) {
    throw new Error('Image too large');
  }

  return new File(
    [blob],
    'share.jpg',
    { type: 'image/jpeg', lastModified: Date.now() }
  );
}
📌 Never fetch image during share tap 📌 Must return a File, not Blob

2️⃣ Share Button Gate (Fail Fast)
function canShare(file, text) {
  return (
    file instanceof File &&
    file.type === 'image/jpeg' &&
    file.size <= 600 * 1024 &&
    typeof text === 'string' &&
    text.length > 0 &&
    navigator.share &&
    navigator.clipboard &&
    window.top === window.self &&
    !document.hidden
  );
}
If canShare() fails → disable button

3️⃣ Gold Share Handler (DO NOT MODIFY)
function handleNativeShare({ file, text }) {
  if (!canShare(file, text)) {
    showFallbackUX();
    return;
  }

  const isIOS = /iPhone|iPad|iPod/.test(navigator.userAgent);

  // 🔒 DIAGNOSTIC LOG (DO NOT REMOVE)
  console.log('[SHARE]', {
    isIOS,
    isFile: file instanceof File,
    type: file.type,
    size: file.size,
    name: file.name
  });

  try {
    // 1️⃣ Clipboard FIRST (synchronous)
    navigator.clipboard.writeText(text);

    // 2️⃣ Native share — iOS MUST be files only
    if (isIOS) {
      navigator.share({ files: [file] });
    } else {
      navigator.share({ files: [file], text });
    }

  } catch (err) {
    console.error('Native share failed', err);
    showFallbackUX();
  }
}
🚫 Forbidden inside this function
* async
* await
* .then
* setTimeout
* analytics calls
* state updates
This is a gesture-critical path.

4️⃣ Expected UX (Correct Behavior)
iOS Safari
1. User taps Share
2. Native share sheet opens
3. User selects WhatsApp / XHS
4. App opens with image already attached
5. “Allow Paste?” appears
6. Text auto-pastes after approval
Android Chrome
Same, minus paste permission prompt.

5️⃣ Kill-Switch Fallback UX (MANDATORY)
function showFallbackUX() {
  // 1. Copy text as best effort
  navigator.clipboard?.writeText?.(shareText);

  // 2. Show modal
  openModal({
    title: 'Share manually',
    steps: [
      '1. Save the image',
      '2. Open WhatsApp / 小红书',
      '3. Paste the text and select the image'
    ],
    actions: [
      { label: 'Save Image', action: saveImage },
      { label: 'Copy Text', action: () => navigator.clipboard.writeText(shareText) }
    ]
  });
}
🚫 Never silently fail 🚫 Never open picker without explanation

6️⃣ What ShareLah Is Definitely Doing (Validated)
* Uses File, not Blob
* Uses navigator.share
* Uses clipboard for text only
* Uses strict iOS payload
* Avoids iframe/webview
* Does not rely on timing hacks

7️⃣ Regression Warning (Pin This)
❗ If someone:
* merges payloads
* removes iOS branching
* moves clipboard after share
* replaces File with Blob
Sharing will break again.
This is browser-behavioral, not code style.

8️⃣ Final Acceptance Checklist (Sign-off)
Only approve when:
* instanceof File === true
* iOS share payload = { files } only
* Clipboard runs before share
* No photo picker appears
* Paste prompt appears
* Fallback UX exists


